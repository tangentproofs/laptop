import LaPToP.Exercises.Basic

namespace LaPToP.Exercises.Ch0

open LaPToP.Exercises

/-- aPToP Exercise 0: There are four cards on a table showing symbols D, E, 2, and 3 (one per card). Each card -/
theorem exercise_0 : Statement 0 := by
  -- exercise 0: statement only, proof deferred
  sorry

/-- aPToP Exercise 1: Jack is looking at Anne. Anne is looking at George. Jack is married. George is single. -/
theorem exercise_1 : Statement 1 := by
  -- exercise 1: statement only, proof deferred
  sorry

/-- aPToP Exercise 2: Here are three statements. -/
theorem exercise_2 : Statement 2 := by
  -- exercise 2: statement only, proof deferred
  sorry

/-- aPToP Exercise 3: Here are ve statements. -/
theorem exercise_3 : Statement 3 := by
  -- exercise 3: statement only, proof deferred
  sorry

end LaPToP.Exercises.Ch0

/-!
# Exercises — Introduction

Statement-only stubs for aPToP §10.0 (book Chapter 10). Each theorem names an
exercise; the body is `sorry` with an explicit deferred-proof marker.
**No proofs** are attempted here. Already-formalized exercises elsewhere in
`LaPToP/` keep their real developments; these stubs are the Chapter 10
inventory.
-/

