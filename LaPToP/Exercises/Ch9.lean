import LaPToP.Exercises.Basic

namespace LaPToP.Exercises.Ch9

open LaPToP.Exercises

/-- aPToP Exercise 495: Express the program -/
theorem exercise_495 : Statement 495 := by
  -- exercise 495: statement only, proof deferred
  sorry

/-- aPToP Exercise 496: √ Suppose a and b are integer boundary variables, x and y are integer interactive
Already has a formal development elsewhere in `LaPToP/` (Exercise 496). -/
theorem exercise_496 : Statement 496 := by
  -- exercise 496: statement only, proof deferred
  sorry

/-- aPToP Exercise 497: √ (grow slow) Suppose alloc allocates 1 unit of memory space and takes time 1 to do
Already has a formal development elsewhere in `LaPToP/` (Exercise 497). -/
theorem exercise_497 : Statement 497 := by
  -- exercise 497: statement only, proof deferred
  sorry

/-- aPToP Exercise 498: Let a and b be boundary variables; assignment to them takes time 0 . Let assignment -/
theorem exercise_498 : Statement 498 := by
  -- exercise 498: statement only, proof deferred
  sorry

/-- aPToP Exercise 499: Let a and b be binary interactive variables. De ne -/
theorem exercise_499 : Statement 499 := by
  -- exercise 499: statement only, proof deferred
  sorry

/-- aPToP Exercise 500: The Substitution Law does not work for interactive variables. -/
theorem exercise_500 : Statement 500 := by
  -- exercise 500: statement only, proof deferred
  sorry

/-- aPToP Exercise 501: According to the de nition of assignment to an interactive variable, writing to the -/
theorem exercise_501 : Statement 501 := by
  -- exercise 501: statement only, proof deferred
  sorry

/-- aPToP Exercise 502: √ (thermostat) Specify a thermostat for a gas burner. The thermostat operates concurrently
Already has a formal development elsewhere in `LaPToP/` (Exercise 502). -/
theorem exercise_502 : Statement 502 := by
  -- exercise 502: statement only, proof deferred
  sorry

/-- aPToP Exercise 503: (interactive data transformation; research) Section 7.2 presented data transformation for -/
theorem exercise_503 : Statement 503 := by
  -- exercise 503: statement only, proof deferred
  sorry

/-- aPToP Exercise 504: (telephone) Specify the control of a simple telephone. Its inputs are those actions you -/
theorem exercise_504 : Statement 504 := by
  -- exercise 504: statement only, proof deferred
  sorry

/-- aPToP Exercise 505: (consensus) Some concurrent processes are connected in a ring. Each process has a local -/
theorem exercise_505 : Statement 505 := by
  -- exercise 505: statement only, proof deferred
  sorry

/-- aPToP Exercise 506: Many programming languages require a variable for input, with a syntax such as read x . -/
theorem exercise_506 : Statement 506 := by
  -- exercise 506: statement only, proof deferred
  sorry

/-- aPToP Exercise 507: Write a program to print the sequence of natural numbers, one per time unit. -/
theorem exercise_507 : Statement 507 := by
  -- exercise 507: statement only, proof deferred
  sorry

/-- aPToP Exercise 508: Write a program to repeatedly print the current time, up until some given time. -/
theorem exercise_508 : Statement 508 := by
  -- exercise 508: statement only, proof deferred
  sorry

/-- aPToP Exercise 509: The equation -/
theorem exercise_509 : Statement 509 := by
  -- exercise 509: statement only, proof deferred
  sorry

/-- aPToP Exercise 510: From the xed-point equation -/
theorem exercise_510 : Statement 510 := by
  -- exercise 510: statement only, proof deferred
  sorry

/-- aPToP Exercise 511: (generate) Write a program to print all texts consisting of only the characters “a” , “b” , -/
theorem exercise_511 : Statement 511 := by
  -- exercise 511: statement only, proof deferred
  sorry

/-- aPToP Exercise 512: (T-strings) Let us call a string S: *(“a”, “b”, “c”) a T-string if no two adjacent nonempty -/
theorem exercise_512 : Statement 512 := by
  -- exercise 512: statement only, proof deferred
  sorry

/-- aPToP Exercise 513: According to the de nition of value expression given in Subsection 5.5.0, what happens -/
theorem exercise_513 : Statement 513 := by
  -- exercise 513: statement only, proof deferred
  sorry

/-- aPToP Exercise 514: (reformat) Write a program to read, reformat, and write a sequence of characters. The -/
theorem exercise_514 : Statement 514 := by
  -- exercise 514: statement only, proof deferred
  sorry

/-- aPToP Exercise 515: (matrix multiplication) Write a program to multiply two n×n matrices that uses n2 -/
theorem exercise_515 : Statement 515 := by
  -- exercise 515: statement only, proof deferred
  sorry

/-- aPToP Exercise 516: (input implementation) Let W be “wait for input on channel c and then read it”.
Already has a formal development elsewhere in `LaPToP/` (Exercise 516). -/
theorem exercise_516 : Statement 516 := by
  -- exercise 516: statement only, proof deferred
  sorry

/-- aPToP Exercise 517: (input with timeout) As in Exercise 516, let W be “wait for input on channel c and then -/
theorem exercise_517 : Statement 517 := by
  -- exercise 517: statement only, proof deferred
  sorry

/-- aPToP Exercise 518: Here are two de nitions. -/
theorem exercise_518 : Statement 518 := by
  -- exercise 518: statement only, proof deferred
  sorry

/-- aPToP Exercise 519: (perfect shuf e) Write a speci cation for a computation that repeatedly reads an input on -/
theorem exercise_519 : Statement 519 := by
  -- exercise 519: statement only, proof deferred
  sorry

/-- aPToP Exercise 520: De ne relation partmerge: nat→nat→bin as follows: -/
theorem exercise_520 : Statement 520 := by
  -- exercise 520: statement only, proof deferred
  sorry

/-- aPToP Exercise 521: (time merge) We want to repeatedly read an input on either channel c or channel d ,
Already has a formal development elsewhere in `LaPToP/` (Exercise 521). -/
theorem exercise_521 : Statement 521 := by
  -- exercise 521: statement only, proof deferred
  sorry

/-- aPToP Exercise 522: (fairer time merge) This question is the same as the time merge (Exercise 521), but if -/
theorem exercise_522 : Statement 522 := by
  -- exercise 522: statement only, proof deferred
  sorry

/-- aPToP Exercise 523: Let t be an extended natural time variable. Is the following speci cation implementable? -/
theorem exercise_523 : Statement 523 := by
  -- exercise 523: statement only, proof deferred
  sorry

/-- aPToP Exercise 524: In the reaction controller in Subsection 9.1.6, it is supposed that the synchronizer receives -/
theorem exercise_524 : Statement 524 := by
  -- exercise 524: statement only, proof deferred
  sorry

/-- aPToP Exercise 525: In the program -/
theorem exercise_525 : Statement 525 := by
  -- exercise 525: statement only, proof deferred
  sorry

/-- aPToP Exercise 526: (choose) The following picture shows a network of communicating processes. -/
theorem exercise_526 : Statement 526 := by
  -- exercise 526: statement only, proof deferred
  sorry

/-- aPToP Exercise 527: √ (power series multiplication) Write a program to read from channel a an in nite
Already has a formal development elsewhere in `LaPToP/` (Exercise 527). -/
theorem exercise_527 : Statement 527 := by
  -- exercise 527: statement only, proof deferred
  sorry

/-- aPToP Exercise 528: √ Prove that execution of the following program deadlocks.
Already has a formal development elsewhere in `LaPToP/` (Exercise 528). -/
theorem exercise_528 : Statement 528 := by
  -- exercise 528: statement only, proof deferred
  sorry

/-- aPToP Exercise 529: (repetition) Write a program to read an in nite sequence, and after every even number of -/
theorem exercise_529 : Statement 529 := by
  -- exercise 529: statement only, proof deferred
  sorry

/-- aPToP Exercise 530: ( le update) A master le of records and a transaction le of records are to be read, one -/
theorem exercise_530 : Statement 530 := by
  -- exercise 530: statement only, proof deferred
  sorry

/-- aPToP Exercise 531: (mutual exclusion) Process P is an endless repetition of a “non-critical section” PN -/
theorem exercise_531 : Statement 531 := by
  -- exercise 531: statement only, proof deferred
  sorry

/-- aPToP Exercise 532: (synchronous communication) A synchronous communication happens when the sender -/
theorem exercise_532 : Statement 532 := by
  -- exercise 532: statement only, proof deferred
  sorry

/-- aPToP Exercise 533: Section 5.3 de ned and implemented the program wait until w where w is a time. -/
theorem exercise_533 : Statement 533 := by
  -- exercise 533: statement only, proof deferred
  sorry

end LaPToP.Exercises.Ch9

/-!
# Exercises — Interaction

Statement-only stubs for aPToP §10.9 / book Ch.9 (book Chapter 10). Each theorem names an
exercise; the body is `sorry` with an explicit deferred-proof marker.
**No proofs** are attempted here. Already-formalized exercises elsewhere in
`LaPToP/` keep their real developments; these stubs are the Chapter 10
inventory.
-/

